#include <iostream>
#include <string>
#include <vector>


class Parser
{
    private:
        std::vector <std::string> tokens;
        parse(std::string enter_string ,std::vector <std::string> vector_string)
        {

        }
};
