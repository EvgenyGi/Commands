#include <iostream>
#include <string>
#include <vector>


class Parser
{
    private:
        std::vector <std::string> tokens;
        Parser(std::string enter_string ,std::vector <std::string> vector_string)
        {

        }
};
